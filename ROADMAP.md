# Buyg 로드맵

현재 서비스는 정적 페이지(GitHub Pages) + 브라우저 `localStorage` 찜하기만으로
운영 중이다. 아래 3가지 기능은 지금 당장 만들지 않고, 실제 트래픽이 붙어서
"여러 사용자가 동시에 보는 서비스"로서의 투자가 정당화될 때 순서대로 검토한다.

## 도입 기준 지표

아래 중 **하나라도** 충족하면 스케일업 단계 착수를 검토한다 (GA4 기준):

- **DAU 100~200명 유지** — 특정 하루가 아니라 최근 7~14일 평균이 이 구간을
  꾸준히 유지될 때. 일시적 스파이크(카톡 발송 직후 등)는 제외.
- **일 찜하기 클릭 수 30~50회 이상** — `fav_btn` 클릭 이벤트(GA4에 아직
  미연동 - 아래 0번 항목 참고) 기준. 찜 자체가 "다시 볼 만큼 가치를 느꼈다"는
  가장 강한 신호라, DAU가 기준에 못 미쳐도 이 지표가 먼저 넘으면 착수해도 된다.

두 지표 모두 현재는 측정 수단이 없다 (`view_page.html`에 GA4는 붙어 있지만
`fav_btn` 클릭을 커스텀 이벤트로 보내고 있지 않음) - 스케일업 검토 이전에
**0) 찜하기 클릭 GA4 이벤트 연동**이 선행돼야 위 두 기준을 실제로 측정할 수
있다.

## 스케일업 단계 구현 기능

### 1) 중앙 집중형 찜 카운터 (소셜 프루프)

- **무엇을**: 카드마다 "❤️ N명이 찜함" 표기. 지금은 찜이 각자 브라우저의
  `localStorage`에만 저장돼 전체 인원수를 알 방법이 없다.
- **어떻게**: Firebase(Firestore) 또는 Supabase에 `(row_key) -> count` 문서
  하나씩. 찜 버튼 클릭 시 해당 문서를 +1/-1 (트랜잭션 또는 카운터 전용
  Cloud Function으로 어뷰징 방지). 읽기는 실시간 구독 없이 페이지 로드 시
  1회 배치 조회로 충분(카드 수백 개를 실시간 구독하면 비용/복잡도만 커짐).
- **전제 조건**: 지금은 서버가 전혀 없는 순수 정적 사이트라 이 순간부터
  "백엔드가 있는 서비스"로 전환된다 - 인증 없는 쓰기 요청을 어떻게 막을지
  (rate limit, App Check 등)를 먼저 설계해야 한다.

### 2) 브라우저 웹 푸시(Web Push) 알림

- **무엇을**: 찜한 공구의 마감일 당일 아침(예: 08:00) "오늘 마감이에요!"
  푸시 발송.
- **어떻게**: Service Worker 등록 + Web Push 구독(Firebase Cloud Messaging
  권장 - VAPID 키 발급, 순수 Web Push 표준도 가능하나 크로스브라우저 대응이
  더 번거로움) + 구독-찜 매핑을 저장할 서버/DB(1번 항목과 같은 백엔드를
  공유해도 됨) + 매일 아침 배치로 "오늘 마감 & 찜됨" 목록을 뽑아 발송하는
  스케줄러(이 프로젝트의 기존 `run_nightly_pipeline.ps1`/Task Scheduler
  패턴을 그대로 재사용 가능).
- **주의**: iOS Safari는 PWA로 홈 화면에 추가한 경우에만 웹 푸시를 지원한다
  (iOS 16.4+) - 국내 트래픽에서 iOS 비중이 높으면 도달률이 기대보다 낮을 수
  있음을 감안해야 한다.

### 3) 카카오 알림톡/친구톡 연동

- **무엇을**: 카카오 간편 로그인으로 사용자를 식별한 뒤, 그 사용자가 찜한
  공구의 마감을 카카오톡 알림톡/친구톡으로 발송.
- **어떻게**: 카카오 디벨로퍼스에 로그인(REST API) 연동 + 알림톡은 비즈메시지
  발송 대행사(카카오워크가 아니라 알리고/솔라피 등 CMS 필요, 알림톡은 사전
  승인된 템플릿만 발송 가능) 계약이 필요 - 이 프로젝트가 이미 쓰고 있는
  "나에게 보내기"(`generator/send_to_me.py`)와는 별개의, 더 무거운 채널이다.
- **선행 작업**: 카카오 로그인으로 사용자를 식별해야 "누가 무엇을 찜했는지"를
  서버가 알 수 있으므로, 1번(중앙 집중형 찜) 없이는 이 기능 자체가 불가능하다
  - 즉 **1 → (2 또는 3)** 순서로 갈 수밖에 없다.

## 순서 원칙

1번(찜 카운터)이 나머지 두 기능의 공통 전제(서버/DB, 사용자-찜 매핑)를
깔아주므로 사실상 선행 단계다. 2번과 3번은 서로 독립적이라 리소스가 허락하는
쪽부터 먼저 붙여도 된다 - 다만 3번은 비즈메시지 계약·템플릿 심사 등 외부
리드타임이 있어 실제 착수는 2번보다 오래 걸릴 가능성이 높다.
